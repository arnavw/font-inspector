{
  "name": "font-inspector",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node esbuild.mjs && cp -r static/* dist/",
    "watch": "node esbuild.mjs --watch"
  },
  "dependencies": {
    "opentype.js": "^1.3.4"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0"
  }
}
